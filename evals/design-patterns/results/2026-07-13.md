# design-patterns forward-test results - 2026-07-13

## Scope

These results exercise the revised skill after splitting the category references into one file per pattern and tightening the trigger description. Each application test was answered by a fresh independent agent that received `SKILL.md`, one prompt, and instructions to open only the references selected by the router. Expected criteria and other agents' answers were withheld.

The metadata test received only the YAML frontmatter.

## Deterministic checks

| Check | Result |
|---|---|
| `python3 /Users/fourcolors/.codex/skills/.system/skill-creator/scripts/quick_validate.py skills/design-patterns` | Pass - `Skill is valid!` |
| `node evals/design-patterns/run.mjs` | Pass - 23 references and 20 adversarial cases |
| `npx skills add . --list` | Pass - `design-patterns` discovered with the revised trigger description |
| `git diff --check` | Pass |

## Fresh-agent forward tests

| Test | Main criteria | Result |
|---|---|---|
| `post_no_pattern` | Reject Factory, Strategy, and Visitor for a fixed 90-line CSV conversion; prefer plain functions and name the absent variation | Pass |
| `post_lookalike` | Distinguish Virtual/Caching Proxy, Decorator-like retry/tracing, and Adapter error/interface translation; avoid a wrapper-class explosion; cover concurrency, retry, cancellation, cache keys, initialization, and cleanup | Pass |
| `post_command` | Treat Command as the message boundary rather than the durability mechanism; require versioned envelopes, at-least-once delivery, idempotency, ambiguous-payment reconciliation, compensation, and durable workflow step state | Pass |
| `post_memento` | Prevent mutable aliasing on save and restore; bound history; validate owner/version/schema; preserve failed restore points; account for concurrent collaborative edits | Pass |
| `post_composite` | Remove child mutation from the leaf contract; use iterative traversal for million-node depth; prevent/detect cycles; define mutation policy, ownership transfer, and idempotent cleanup | Pass |
| `post_trigger_boundary` | Classify 12 metadata-only prompts spanning positive GoF requests and negative UI, schema, language-protocol, architecture, React-state, and non-software uses | Pass - 12/12 |

## Observations

- Progressive disclosure worked as intended: application agents opened the router plus only one to three directly relevant pattern references.
- The no-pattern guardrail survived a prompt that explicitly proposed three named patterns.
- The safety tests did not attribute durability, atomicity, thread safety, snapshot isolation, or family consistency to a GoF pattern by itself.
- The deterministic runner validates the fixture schema, reference inventory, links, metadata boundaries, code-fence balance, and removal of the unsupported ranking. It does not judge model output automatically; the fresh-agent results above are the preserved forward-test evidence.
